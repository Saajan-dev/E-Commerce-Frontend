const siteLogo: string = import.meta.env.VITE_SITE_LOGO || "";
const baseUrl: string = import.meta.env.VITE_BASE_URL;
const cookie_user_data = "USER DETAILS";

export { baseUrl, cookie_user_data, siteLogo };
